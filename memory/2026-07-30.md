# 2026-07-30 작업 로그

## 노트북 인계 저장소 반영

- 대상: `https://github.com/sanghyun747/laptop-desktop.git`
- 기존 checkout은 다른 EBSi 변경이 있고 원격 main보다 뒤처져 있어
  수정하지 않았다.
- 원격 main `9d9e51e...`에서 별도
  `cofathon-track2-sync-20260730` worktree/브랜치를 만들었다.
- Cofathon 자료를 `cofathon-oliveyoung/` 아래로 분리했다.
- 대기실 중복 스크린샷은 SHA-256이 같아 하나만 보관했다.
- 원문 채팅, credential, cache, ZIP, 과거 agent-loop 전체 복제본,
  dirty G2 작업 트리는 제외했다.
- contest-kit의 280분 고정값을 225분으로 바꾸고 제출 후 3분 질문,
  연장 미가정, 마지막 10분 제출 reserve를 검증하도록 수정했다.
- 과거 준비·검증 문서는 삭제하지 않고 `archive/`에 경고를 붙였다.
- G1의 정확한 태그와 peeled commit을 기록했다.

## 검증

- Windows Python 3.12.10 compile: PASS
- contest-kit unit tests: 4/4 PASS
- preflight: PASS, phase total 225, secret values not read
- 두 deterministic bundle의 SHA-256 일치:
  `7f6d6cf46fa3fc7cf5db939bd533a9d177a7b0c5432652c6b28a1c9b33cb7735`
- 별도 secret pattern scan: no matches
- `git diff --check`: PASS
- WSL 교차 검증은 현재 Windows 호스트에 WSL 배포판이 설치되지 않아
  수행하지 못했다. Linux 호환성은 표준 라이브러리와 POSIX 명령
  예시를 유지했지만 웹 IDE 입장 후 smoke가 필요하다.
- 원격 main에
  `d781fd7031fe0d96755e1fa03096bfbd612f19d2`를 fast-forward push했다.
- G1 annotated tag
  `checkpoint/b1-g1-materialization-custody-20260728`도 원격에 게시했다.
- `C:\Users\sangh\checkpoints\cofathon-remote-pass-20260730-d781fd7`에
  새 shallow clone을 남겼다. clone HEAD와 source HEAD가 일치했고
  preflight 및 4/4 tests가 다시 PASS했다.

## 실패와 복구 포인트

- 파일 크기 진단 중 PowerShell에서 `foreach (...) { ... } |` 형태를
  두 번 사용해 parser error가 났다. 둘 다 실행 전 실패해 파일 상태는
  바뀌지 않았다.
- 이후 `$rows`에 먼저 모은 뒤 별도로 파이프하는 형태로 제한했다.
- clean-clone 검증에서 짧은 커밋 `d781fd7`의 전체 SHA를 추정해
  첫 비교식이 실패했다. clone은 정상 생성됐고, 이후 source와 clone의
  `git rev-parse HEAD` 결과를 직접 비교해 통과했다.
- 복구 기준은 원격 main `9d9e51e...`; 최종 커밋이 새 체크포인트다.
